excluded:
  - Pods
  - WalkJourney/AppDelegate.swift
  - WalkJourney/SceneDelegate.swift
disabled_rules:
  - trailing_whitespace
identifier_name:
  excluded: # excluded via string array
    - id
    - URL
    - url
    - app
file_length:
  warning: 500
  error: 800
type_body_length:
  - 500 # warning
  - 700 # error
opt_in_rules:
  - force_unwrapping